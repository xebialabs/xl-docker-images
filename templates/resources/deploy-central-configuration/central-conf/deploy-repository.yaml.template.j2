xl.repository:
  artifacts:
    type: "db"
  database:
    db-driver-classname: "${XL_DB_DRIVER}"
    db-url: "${XL_DB_URL}"
    db-password: |-
      ${XL_DB_PASSWORD}
    db-username: "${XL_DB_USERNAME}"
    max-pool-size: ${XL_DB_MAX_POOL_SIZE}
